/// Initialize button state and default colors
// Default dimensions (can be overridden by children)
size_x = 80; // Width of the button
size_y = 32;  // Height of the button
target_scale = 1;

// Button states
hovering = false;
clicked = false;

// Default colors
default_color = c_white;       // Normal button color
hover_color = c_gray;          // Hover color
clicked_color = c_black;       // Clicked color

// Create Event
activate_button = function() 
{
	
}
